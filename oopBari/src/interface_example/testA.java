package interface_example;

public interface testA {
    
    void print();
}

interface marker {
    /*empty interface called marker interface*/
    /*if the last three character is like "able" then this is by default interface (like: Cloneable, Reloadable)*/
}
